extend layout 

block content
    h1 #{title}
    p Math.fround() is applied to #{number1} is #{fround}
    p Math.random() is applied to  range 0 to 10 is #{random}
    p Math.round() is applied to #{number2} is #{round}